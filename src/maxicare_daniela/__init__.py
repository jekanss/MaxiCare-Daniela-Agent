"""Paquete del proyecto de agentes.

Renombra esta carpeta (`paquete_agente`) al `meta.nombre_proyecto` de `plan-agentes.json`
en snake_case cuando copies la plantilla para un cliente concreto. Este archivo no importa
nada de sus submódulos a propósito: quien necesite `agentes`, `herramientas`, `contratos`,
`runtime` o `config` los importa por su nombre explícito, para que la frontera entre
módulos quede visible en cada import y no escondida detrás de un `__init__.py` que
reexporta todo.
"""
